# git-cliff configuration for openclaw-sdk-go

[changelog]
header = """# Changelog

All notable changes to this project will be documented in this file.

"""
body = """{% if version %}## {{ version }}({{ timestamp | date(format="%Y-%m-%d") }})
{% else %}## Unreleased{% endif %}

{% for c in commits %}{% if c.breaking %}### 🚨 BREAKING CHANGES

{% for c in commits %}{% if c.breaking %}{% set msg = c.message | split(pat="\n") | first | trim | upper_first %}- {{ msg }} ([{{ c.id | truncate(length=7, end="") }}]({{ c.id }})) - ({{ c.author.name }})
{% endif %}{% endfor %}
{% break %}{% endif %}{% endfor %}

{% for group, clist in commits | group_by(attribute="group") %}{% if group == "A Features" %}{% if clist | length > 0 %}### 🚀 Features

{% for c in clist %}{% if not c.breaking %}{% set msg = c.message | split(pat="\n") | first | trim | upper_first %}- {{ msg }} ([{{ c.id | truncate(length=7, end="") }}]({{ c.id }})) - ({{ c.author.name }})
{% endif %}{% endfor %}{% endif %}{% endif %}{% endfor %}
{% for group, clist in commits | group_by(attribute="group") %}{% if group == "B Bug Fixes" %}{% if clist | length > 0 %}### 🐛 Bug Fixes

{% for c in clist %}{% if not c.breaking %}{% set msg = c.message | split(pat="\n") | first | trim | upper_first %}- {{ msg }} ([{{ c.id | truncate(length=7, end="") }}]({{ c.id }})) - ({{ c.author.name }})
{% endif %}{% endfor %}{% endif %}{% endif %}{% endfor %}
{% for group, clist in commits | group_by(attribute="group") %}{% if group == "C Security" %}{% if clist | length > 0 %}### 🔒 Security

{% for c in clist %}{% if not c.breaking %}{% set msg = c.message | split(pat="\n") | first | trim | upper_first %}- {{ msg }} ([{{ c.id | truncate(length=7, end="") }}]({{ c.id }})) - ({{ c.author.name }})
{% endif %}{% endfor %}{% endif %}{% endif %}{% endfor %}
{% for group, clist in commits | group_by(attribute="group") %}{% if group == "D Performance" %}{% if clist | length > 0 %}### ⚡ Performance

{% for c in clist %}{% if not c.breaking %}{% set msg = c.message | split(pat="\n") | first | trim | upper_first %}- {{ msg }} ([{{ c.id | truncate(length=7, end="") }}]({{ c.id }})) - ({{ c.author.name }})
{% endif %}{% endfor %}{% endif %}{% endif %}{% endfor %}
{% for group, clist in commits | group_by(attribute="group") %}{% if group == "E Refactor" %}{% if clist | length > 0 %}### ♻️ Refactor

{% for c in clist %}{% if not c.breaking %}{% set msg = c.message | split(pat="\n") | first | trim | upper_first %}- {{ msg }} ([{{ c.id | truncate(length=7, end="") }}]({{ c.id }})) - ({{ c.author.name }})
{% endif %}{% endfor %}{% endif %}{% endif %}{% endfor %}
{% for group, clist in commits | group_by(attribute="group") %}{% if group == "F Testing" %}{% if clist | length > 0 %}### ✅ Testing

{% for c in clist %}{% if not c.breaking %}{% set msg = c.message | split(pat="\n") | first | trim | upper_first %}- {{ msg }} ([{{ c.id | truncate(length=7, end="") }}]({{ c.id }})) - ({{ c.author.name }})
{% endif %}{% endfor %}{% endif %}{% endif %}{% endfor %}
{% for group, clist in commits | group_by(attribute="group") %}{% if group == "G Styling" %}{% if clist | length > 0 %}### 💄 Styling

{% for c in clist %}{% if not c.breaking %}{% set msg = c.message | split(pat="\n") | first | trim | upper_first %}- {{ msg }} ([{{ c.id | truncate(length=7, end="") }}]({{ c.id }})) - ({{ c.author.name }})
{% endif %}{% endfor %}{% endif %}{% endif %}{% endfor %}
{% for group, clist in commits | group_by(attribute="group") %}{% if group == "H Build" %}{% if clist | length > 0 %}### 🔨 Build

{% for c in clist %}{% if not c.breaking %}{% set msg = c.message | split(pat="\n") | first | trim | upper_first %}- {{ msg }} ([{{ c.id | truncate(length=7, end="") }}]({{ c.id }})) - ({{ c.author.name }})
{% endif %}{% endfor %}{% endif %}{% endif %}{% endfor %}
{% for group, clist in commits | group_by(attribute="group") %}{% if group == "I Documentation" %}{% if clist | length > 0 %}### 📖 Documentation

{% for c in clist %}{% if not c.breaking %}{% set msg = c.message | split(pat="\n") | first | trim | upper_first %}- {{ msg }} ([{{ c.id | truncate(length=7, end="") }}]({{ c.id }})) - ({{ c.author.name }})
{% endif %}{% endfor %}{% endif %}{% endif %}{% endfor %}
{% for group, clist in commits | group_by(attribute="group") %}{% if group == "Z Miscellaneous Tasks" %}{% if clist | length > 0 %}### 🔧 Miscellaneous Tasks

{% for c in clist %}{% if not c.breaking %}{% set msg = c.message | split(pat="\n") | first | trim | upper_first %}- {{ msg }} ([{{ c.id | truncate(length=7, end="") }}]({{ c.id }})) - ({{ c.author.name }})
{% endif %}{% endfor %}{% endif %}{% endif %}{% endfor %}

{% if previous %}{% if previous.version and version %}
**Full Changelog:** [{{ previous.version }}...{{ version }}](https://github.com/frisbee-ai/openclaw-sdk-go/compare/{{ previous.version }}...{{ version }}){% endif %}{% endif %}

"""
footer = """<!-- generated by git-cliff -->"""
trim = true

[git]
conventional_commits = true
filter_unconventional = false
protect_breaking_commits = true
commit_parsers = [
    { message = "^chore", skip = true },
    { message = "^feat", group = "A Features" },
    { message = "^fix", group = "B Bug Fixes" },
    { message = "^security", group = "C Security" },
    { message = "^perf", group = "D Performance" },
    { message = "^refactor", group = "E Refactor" },
    { message = "^test", group = "F Testing" },
    { message = "^style", group = "G Styling" },
    { message = "^build", group = "H Build" },
    { message = "^doc", group = "I Documentation" },
    { message = "^ci", group = "Z Miscellaneous Tasks" },
    { body = ".*security", group = "C Security" },
    { message = "^chore\\(deps.*\\)", skip = true },
    { message = "^chore\\(pr\\)", skip = true },
    { message = "^chore\\(pull\\)", skip = true },
    { message = "^chore\\(release\\): prepare for", skip = true },
]
filter_commits = false
topo_order = false
sort_commits = "oldest"
