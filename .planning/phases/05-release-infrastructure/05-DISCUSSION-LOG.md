# Phase 5: Release Infrastructure - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-29
**Phase:** 05-release-infrastructure
**Areas discussed:** GoReleaser mode, tag strategy, CHANGELOG tool, CI release job

---

## GoReleaser Distribution Mode (REL-01)

| Option | Description | Selected |
|--------|-------------|----------|
| `mode: github` | Publish to GitHub Releases with notes | ✓ |
| `mode: replace` | Replace existing release content | |
| `mode: blobs` | Push to blob storage | |

**User's choice:** `mode: github` — GitHub Release, not blobs
**Notes:** User confirmed GitHub Release mode. Current config has `mode: replace` which needs updating.

---

## Tag Strategy and Initial Version (REL-02)

| Option | Description | Selected |
|--------|-------------|----------|
| Start from v0.1.0 | Begin semantic versioning from v0.1.0 | |
| Use v1.0.1 (patch bump) | v1.0.0 tag already exists at 3e27f32, next is patch | ✓ |

**User's choice:** `v1.0.1` — v1.0.0 already exists at commit 3e27f32
**Notes:** v1.0.0 tag exists at an earlier commit (3e27f32). ~9 commits of Phase 3 + Phase 4 work exist after it. Next version should be v1.0.1 patch bump.

---

## CHANGELOG Automation Tool (REL-03)

| Option | Description | Selected |
|--------|-------------|----------|
| git-cliff | Go-native, flexible config, PR-level granularity | ✓ |
| release-please | Google tool, GitHub Actions integration, auto-PR | |

**User's choice:** git-cliff — configuration file is `cliff.toml`
**Notes:** User confirmed: git-cliff with `cliff.toml` at repo root. Already fully configured. CHANGELOG.md exists with `<!-- generated by git-cliff -->` marker.

---

## CI Release Job

| Option | Description | Selected |
|--------|-------------|----------|
| `.github/workflows/release.yml` already exists | Full goreleaser + git-cliff integration | ✓ |
| Create new release job | N/A | |

**User's choice:** Use existing `.github/workflows/release.yml`
**Notes:** User confirmed release workflow already exists and is properly configured. Triggers on `v*.*.*` tags, integrates git-cliff for release notes, uses goreleaser-action v7.

---

## GoReleaser before.hooks

| Option | Description | Selected |
|--------|-------------|----------|
| Keep all hooks (mod tidy, generate, test) | Current config | |
| Remove `go test ./...` from hooks | CI already runs tests | ✓ |
| Remove `go generate ./...` if no directives | Check first | |

**Notes:** Pending: Check if project has `//go:generate` directives before including `go generate ./...` in hooks.

---

## Claude's Discretion

The following are delegated to researcher/planner judgment:

- Exact `before.hooks` cleanup — check for `//go:generate` directives, remove redundant `go test`
- Whether to add a pre-commit hook to update CHANGELOG.md on each commit
- Whether `prerelease: auto` is the right choice for v1.0.1 or should be explicit `prerelease: false`

---

*Discussion log: 2026-03-29*
